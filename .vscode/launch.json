{
    "configurations": [
        {
            "type": "go",
            "request": "launch",
            "name": "Launch",
            "mode": "exec",
            "program": "${workspaceFolder}/build/nltst_scheduler",
            "preLaunchTask": "Build Go Application",
            "cwd": "${workspaceFolder}"
        }
    ]
}