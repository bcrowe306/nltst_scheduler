package components


templ Icon(name string, width int, height int, fill string) {
    <svg class="bi" width={ width } height={ height } fill={ fill } xmlns="http://www.w3.org/2000/svg">
        <use xlink:href={ "/public/icons/" +  name + ".svg" } />
    </svg>
}